import telebot
import json
import os
from datetime import datetime, timedelta
from telebot.types import ReplyKeyboardMarkup, KeyboardButton
from flask import Flask
import threading
import random

TOKEN = os.getenv('TOKEN')
bot = telebot.TeleBot(TOKEN)

# Словарь для обработки сокращений
days_map = {
    'понедельник': 'понедельник', 'пн': 'понедельник', '1': 'понедельник', 'пон': 'понедельник',
    'вторник': 'вторник', 'вт': 'вторник', '2': 'вторник',
    'среда': 'среда', 'ср': 'среда', '3': 'среда',
    'четверг': 'четверг', 'чт': 'четверг', '4': 'четверг',
    'пятница': 'пятница', 'пт': 'пятница', '5': 'пятница', 'птн': 'пятница'
}

# Расписание только учебные дни
schedule = {
  "понедельник": [
    {"время": "8:00-8:40", "урок": "🎯 Внеурочка: Разговоры о важном"},
    {"время": "9:00-9:40", "урок": "📖 Литературное чтение"},
    {"время": "10:00-10:40", "урок": "📝 Русский язык"},
    {"время": "10:50-11:30", "урок": "🔢 Математика"},
    {"время": "11:50-12:30", "урок": "🧠 Внеурочка: Функциональная грамотность"}
  ],
  "вторник": [
    {"время": "8:00-8:40", "урок": "📖 Литературное чтение"},
    {"время": "9:00-9:40", "урок": "📝 Русский язык"},
    {"время": "10:00-10:40", "урок": "♟️ Шахматы"},
    {"время": "10:50-11:30", "урок": "🌍 Окружающий мир"},
    {"время": "11:50-12:30", "урок": "🔢 Внеурочка: Математика"}
  ],
  "среда": [
    {"время": "8:00-8:40", "урок": "🏃‍♂️ Внеурочка: Физкультура"},
    {"время": "9:00-9:40", "урок": "🎵 Внеурочка: Музыка"},
    {"время": "10:00-10:40", "урок": "🔢 Математика"},
    {"время": "10:50-11:30", "урок": "📝 Русский язык"},
    {"время": "11:50-12:30", "урок": "🔨 Труд"}
  ],
  "четверг": [
    {"время": "8:00-8:40", "урок": "📖 Литературное чтение"},
    {"время": "9:00-9:40", "урок": "📝 Русский язык"},
    {"время": "10:00-10:40", "урок": "🔢 Математика"},
    {"время": "10:50-11:30", "урок": "🎨 Внеурочка: ИЗО"},
    {"время": "11:50-12:30", "урок": "🏛️ Внеурочка: Моя Москва"}
  ],
  "пятница": [
    {"время": "8:00-8:40", "урок": "📖 Литературное чтение"},
    {"время": "9:00-9:40", "урок": "📝 Русский язык"},
    {"время": "10:00-10:40", "урок": "🌍 Окружающий мир"},
    {"время": "10:50-11:30", "урок": "🏃‍♂️ Внеурочка: Физкультура"},
    {"время": "11:50-12:30", "урок": "🔢 Внеурочка: Математика"}
  ]
}

print("✅ Расписание учебной недели загружено!")

# ✅ ИСПРАВЛЕННАЯ КЛАВИАТУРА: 2 кнопки в ряд + ТЕКУЩАЯ неделя
def get_days_keyboard():
    today = datetime.now()
    russian_days = ["Понедельник", "Вторник", "Среда", "Четверг", "Пятница"]
    
    markup = ReplyKeyboardMarkup(resize_keyboard=True, row_width=2)
    
    # Текущая неделя: ближайший понедельник этой недели
    days_ahead = (7 - today.weekday()) % 7  # До следующего понедельника
    monday_this_week = today - timedelta(days=today.weekday())
    
    buttons = []
    for i in range(5):  # Пн-Пт текущей недели
        current_day = monday_this_week + timedelta(days=i)
        day_name = russian_days[i]
        date_str = current_day.strftime("%d.%m")
        emojis = ["🔵", "🟢", "🟡", "🟠", "🔴"]
        button_text = f"{emojis[i]} {date_str}\n{day_name}"
        buttons.append(KeyboardButton(button_text))
    
    # 2 кнопки в ряд
    markup.row(buttons[0], buttons[1])
    markup.row(buttons[2], buttons[3])
    markup.row(buttons[4])  # Пятница одна
    
    return markup

# 🚀 СТАРТ
@bot.message_handler(commands=['start'])
def start(message):
    greetings = [
        "🔔 Привет, Тимоша! 📚✨",
        "🎓 Расписание учебной недели:",
        "👋 Выбери день:"
    ]
    bot.reply_to(message, 
        random.choice(greetings),
        reply_markup=get_days_keyboard())

# 🧠 УЛУЧШЕННАЯ ОБРАБОТКА
@bot.message_handler(content_types=['text'])
def handle_day(message):
    day_input = message.text.lower().strip()
    
    # Убираем все НЕ-буквы кроме пробелов
    cleaned = ''.join(c for c in day_input if c.isalpha() or c.isspace())
    cleaned = cleaned.replace('📅', '').replace('🔵', '').replace('🟢', '').replace('🟡', '').replace('🟠', '').replace('🔴', '')
    cleaned = ' '.join(cleaned.split())
    
    # Ищем день недели
    day = None
    for key in days_map:
        if key in cleaned:
            day = days_map[key]
            break
    
    if not day:
        bot.reply_to(message, 
            "❓ Выбери день из кнопок или напиши: пн, вт, ср...",
            reply_markup=get_days_keyboard())
        return
    
    # Расписание
    text = f"📚 <b>Расписание на {day.capitalize()}</b>\n\n"
    for lesson in schedule[day]:
        text += f"⏰ <b>{lesson['время']}</b> — {lesson['урок']}\n"
    
    tips = ["💪 Учись с удовольствием!", "📖 Ты лучший! ✨", "🎯 Всё получится!"]
    text += f"\n<i>{random.choice(tips)}</i>"
    
    bot.reply_to(message, text, parse_mode='HTML', reply_markup=get_days_keyboard())

# /HELP
@bot.message_handler(commands=['help'])
def help_command(message):
    bot.reply_to(message, 
        "🆘 <b>Помощь:</b>\n\n"
        "📅 Кнопки или пиши: пн, вт, понедельник...\n"
        "✨ Работает 24/7!",
        parse_mode='HTML', reply_markup=get_days_keyboard())

# Веб-сервер для Render
app = Flask(__name__)

@app.route("/")
def hello():
    return "🔔 Бот Тимоши работает! 📚"

def run_flask():
    port = int(os.environ.get("PORT", 10000))
    app.run(host="0.0.0.0", port=port, debug=False)

if __name__ == "__main__":
    flask_thread = threading.Thread(target=run_flask)
    flask_thread.daemon = True
    flask_thread.start()
    
    print("🚀 Бот учебной недели запущен!")
    bot.infinity_polling()
